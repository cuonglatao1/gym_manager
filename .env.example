# ===========================================
# GYM MANAGER - Environment Configuration
# ===========================================
# Copy file này thành .env và điền thông tin thực tế

# Server Configuration
NODE_ENV=development
PORT=3000

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gym_manager_simple
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# JWT Configuration
JWT_ACCESS_SECRET=your-very-long-and-secure-access-secret-key-here
JWT_REFRESH_SECRET=your-very-long-and-secure-refresh-secret-key-here

# ===========================================
# HƯỚNG DẪN:
# ===========================================
# 1. Copy file này thành .env: cp .env.example .env
# 2. Thay đổi các giá trị sau:
#    - DB_PASSWORD: Mật khẩu PostgreSQL của bạn
#    - JWT_ACCESS_SECRET: Tạo chuỗi ngẫu nhiên dài tối thiểu 32 ký tự
#    - JWT_REFRESH_SECRET: Tạo chuỗi ngẫu nhiên dài tối thiểu 32 ký tự
# 3. Tạo database PostgreSQL với tên 'gym_manager_simple'
# 4. Chạy: npm run dev

# LƯU Ý BẢO MẬT:
# - KHÔNG commit file .env lên Git
# - Sử dụng secrets phức tạp trong production
# - Thay đổi tất cả mật khẩu mặc định